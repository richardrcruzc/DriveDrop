@using DriveDrop.Bl.Models;
@model  IEnumerable<DriveDrop.Bl.ViewModels.RateDetailModel>
                       @if (Model == null || Model.Count() == 0)
                            {
                           <p>None.</p>
                            }
                            else
                            {
   @Html.EditorFor(m => m)
}

